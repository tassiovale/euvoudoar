apiVersion: apps/v1
kind: Deployment
metadata:
  name: donations-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: donations-mongo
  template:
    metadata:
      labels:
        app: donations-mongo
    spec:
      containers:
        - name: donations-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: donations-mongo-srv
spec:
  selector:
    app: donations-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
